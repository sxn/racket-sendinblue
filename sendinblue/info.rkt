#lang info

(define version "0.1")
(define collection "sendinblue")
(define scribblings '(("sendinblue.scrbl")))

(define deps '("base"))
(define build-deps '("racket-doc"
                     "scribble-lib"))
